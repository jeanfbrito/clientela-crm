<span id="<%= dom_id(task, :panel) %>" class="panel-task">
  <%= link_to_icon_destroy_remote(task) %>
  <%= link_to t("common.edit"), edit_task_url(task, :format => :js), :class => "fancybox-ajax" %>
</span>